## 1. Init Next.js in the existing repo

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

## 2. Install dependencies

```bash
npm install mongoose cloudinary
npm install -D @types/node
```